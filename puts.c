#include <stdio.h>
#include <string.h>

int main()
{
        char str[70];

        strcpy(str, "Programming is like building a multilingual puzzle");

        puts(str);

        return (0);
}

int void()
{
    int digit = 45;
    printf("%d", digit);

    return (0);
}
